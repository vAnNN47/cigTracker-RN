/**
 * DEPRECATED / UNUSED.
 * This was the gorhom + keyboard-controller bridge. We switched to
 * @lodev09/react-native-true-sheet (native sheet, native keyboard handling),
 * so this file is no longer needed. Kept empty only because the sandbox can't
 * delete it — safe to remove from your repo manually.
 */
export {};
