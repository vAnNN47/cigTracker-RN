/**
 * DEPRECATED / UNUSED template file. Our navigation is the Expo Router (tabs)
 * group in src/app/(tabs). Stubbed (it referenced a removed /explore route).
 * Safe to delete from your repo.
 */
export default function AppTabs() {
  return null;
}
